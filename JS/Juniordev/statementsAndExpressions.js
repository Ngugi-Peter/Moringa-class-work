/* Expression is a piece of code that produces a value 
x=5 is an expression because it produces a value of 5
10 + 20 is an expression because it produces a value of 30
circleArea = 3.14 * radius * radius is an expression because it produces a value of area of circle
reference value is an expression because it produces a value of reference to the object in memory
*/

// this is a statement because it does not produce a value
function sayMyName() {
    console.log(Joe);
}

let f1=23; //this is a staement because it does not produce a value
console.log(f1); //this is a statement because it does not produce a value
console.log(f1+10); //
console.log(sayMyName()); //
console.log(f1>23); //function cause it produces undefined value